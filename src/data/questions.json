[
  {
    "id": 1,
    "category": "Javaの基本",
    "question": "Javaプログラムの実行エントリーポイントとなるメソッドの宣言として、正しいものはどれですか。",
    "code": "",
    "options": [
      "public void main(String[] args)",
      "public static void main(String args)",
      "public static void main(String[] args)",
      "static void main(String[] args)"
    ],
    "answerIndex": 2,
    "explanation": "Javaプログラムの起動時（エントリーポイント）には、`public static void main(String[] args)` メソッドが必要です。アクセス修飾子は `public`、静的メソッドを示す `static`、戻り値なしを示す `void`、そして引数としてString配列 `String[] args`（または可変長引数）を受け取る必要があります。"
  },
  {
    "id": 2,
    "category": "データの操作",
    "question": "次のコードを実行したときの出力結果として、正しいものはどれですか。",
    "code": "int a = 10;\nint b = a++;\nSystem.out.println(\"a:\" + a + \", b:\" + b);",
    "options": [
      "a:10, b:10",
      "a:11, b:10",
      "a:11, b:11",
      "a:10, b:11"
    ],
    "answerIndex": 1,
    "explanation": "後置インクリメント演算子（`a++`）は、現在の `a` の値（10）を評価し、変数 `b` に代入した後に、`a` の値を 1 増やします。したがって、`b` には 10 が代入され、その後 `a` は 11 になります。結果として `a:11, b:10` が出力されます。"
  },
  {
    "id": 3,
    "category": "制御構造",
    "question": "次のコードを実行したときの出力結果として、正しいものはどれですか。",
    "code": "int x = 5;\nswitch(x) {\n    case 5:\n        System.out.print(\"5 \");\n    case 10:\n        System.out.print(\"10 \");\n        break;\n    default:\n        System.out.print(\"default\");\n}",
    "options": [
      "5 ",
      "5 10 ",
      "5 10 default",
      "コンパイルエラーが発生する"
    ],
    "answerIndex": 1,
    "explanation": "`x` の値は 5 なので、`case 5:` が実行されます。しかし、`case 5:` には `break` 文が記述されていないため、次の `case 10:` の処理も続けて実行されます（フォールスルー）。`case 10:` の最後には `break` 文があるため、そこで switch 文を抜けます。したがって、出力結果は `5 10 ` となります。"
  },
  {
    "id": 4,
    "category": "配列の操作",
    "question": "次の配列宣言および初期化のうち、コンパイルエラーになるものはどれですか。",
    "code": "",
    "options": [
      "int[] array = new int[3];",
      "int[] array = {1, 2, 3};",
      "int[] array = new int[] {1, 2, 3};",
      "int[3] array = new int[3];"
    ],
    "answerIndex": 3,
    "explanation": "Javaでは、配列の宣言時（左辺）に変数の型宣言としてサイズ（要素数）を指定することはできません。サイズは配列のインスタンス化時（右辺の `new int[3]` など）に指定します。そのため、`int[3] array` という宣言はコンパイルエラーになります。"
  },
  {
    "id": 5,
    "category": "クラスの定義とオブジェクトの使用",
    "question": "次のコードを実行したときの出力結果として、正しいものはどれですか。",
    "code": "class Sample {\n    int val = 10;\n    void change(int val) {\n        val = 20;\n    }\n}\n// 実行部\nSample s = new Sample();\ns.change(50);\nSystem.out.println(s.val);",
    "options": [
      "10",
      "20",
      "50",
      "コンパイルエラーが発生する"
    ],
    "answerIndex": 0,
    "explanation": "`change` メソッド内で `val = 20;` と代入していますが、これはメソッドの引数（ローカル変数）である `val` の値を書き換えているだけであり、インスタンスフィールドである `this.val` の値には影響を与えません。したがって、インスタンスメンバー `s.val` は初期値の 10 のまま変わりません。"
  },
  {
    "id": 6,
    "category": "継承とポリモーフィズム",
    "question": "次のコードを実行したときの出力結果として、正しいものはどれですか。",
    "code": "class Parent {\n    void show() {\n        System.out.println(\"Parent\");\n    }\n}\nclass Child extends Parent {\n    void show() {\n        System.out.println(\"Child\");\n    }\n}\n// 実行部\nParent p = new Child();\np.show();",
    "options": [
      "Parent",
      "Child",
      "コンパイルエラーが発生する",
      "実行時例外が発生する"
    ],
    "answerIndex": 1,
    "explanation": "変数 `p` の宣言型は `Parent` ですが、参照している実際のインスタンスは `Child` クラス of オブジェクトです。Javaでは、メソッドが呼び出された際、オブジェクトの実際の型（ランタイム型）に基づいてオーバーライドされたメソッドが動的に決定され、実行されます（ポリモーフィズム）。したがって、`Child` クラスでオーバーライドされた `show()` メソッドが実行され、出力は `Child` となります。"
  },
  {
    "id": 7,
    "category": "データの操作",
    "question": "次のコードを実行したときの出力結果として、正しいものはどれですか。",
    "code": "int a = 10;\nint b = 3;\nSystem.out.println(a / b);",
    "options": [
      "3.3333333333333335",
      "3",
      "4",
      "コンパイルエラーが発生する"
    ],
    "answerIndex": 1,
    "explanation": "Javaにおいて、`int` 型（整数）同士の算術演算（除算 `/`）の結果は、小数点以下が切り捨てられた `int` 型（整数）になります。したがって、`10 / 3` の結果は `3` となります。もし小数点以下まで計算したい場合は、`(double)a / b` や `10.0 / 3` のように、少なくとも一方を浮動小数点数型（`double` または `float`）にキャストする必要があります。"
  },
  {
    "id": 8,
    "category": "制御構造",
    "question": "次のコードを実行したときの出力結果として、正しいものはどれですか。",
    "code": "int num = 10;\nString result = num > 10 ? \"A\" : num < 10 ? \"B\" : \"C\";\nSystem.out.println(result);",
    "options": [
      "A",
      "B",
      "C",
      "コンパイルエラーが発生する"
    ],
    "answerIndex": 2,
    "explanation": "これは三項演算子のネストです。まず最初の条件式 `num > 10` (10 > 10) は `false` なので、コロン `:` の右側の式 `num < 10 ? \"A\" : num < 10 ? ...` の後半部分である `num < 10 ? \"B\" : \"C\"` が評価されます。ここで、次の条件式 `num < 10` (10 < 10) も `false` なので、さらにコロン `:` の右側にある `\"C\"` が選択されます。結果として `\"C\"` が出力されます。"
  },
  {
    "id": 9,
    "category": "配列の操作",
    "question": "次のコードを実行したときの出力結果として、正しいものはどれですか。",
    "code": "int[][] array = new int[2][];\narray[0] = new int[]{1, 2};\narray[1] = new int[]{3, 4, 5};\nSystem.out.println(array[1][2]);",
    "options": [
      "0",
      "5",
      "NullPointerExceptionが発生する",
      "コンパイルエラーが発生する"
    ],
    "answerIndex": 1,
    "explanation": "Javaの多次元配列は「配列の配列」であり、各行（各要素）で異なる長さの配列を持つこと（ジャグ配列）が可能です。このコードでは、`array[1]` に長さ 3 の配列 `{3, 4, 5}` を代入しています。`array[1][2]` は、2行目（インデックス 1）の3番目の要素（インデックス 2）を指すため、値は `5` となり、正常に出力されます。"
  },
  {
    "id": 10,
    "category": "クラスの定義とオブジェクトの使用",
    "question": "次のクラス定義があるとき、実行部を実行した結果として、正しいものはどれですか。",
    "code": "class A {\n    A(int x) {\n        System.out.print(\"A\");\n    }\n}\n// 実行部\nA a = new A();",
    "options": [
      "何も出力されず正常終了する",
      "「A」が出力される",
      "コンパイルエラーが発生する",
      "実行時に例外が発生する"
    ],
    "answerIndex": 2,
    "explanation": "クラス内に明示的にコンストラクタ（この場合は `A(int x)`）が定義されている場合、コンパイラによる「デフォルトコンストラクタ（引数なしコンストラクタ）」の自動生成は行われません。そのため、実行部で `new A()`（引数なし）と呼び出そうとすると、該当するコンストラクタが存在しないためコンパイルエラーになります。解決するには、明示的に引数なしコンストラクタを定義するか、インスタンス化時に引数を渡す必要があります。"
  },
  {
    "id": 11,
    "category": "クラスの定義とオブジェクトの使用",
    "question": "次のコードを実行したときの出力結果として、正しいものはどれですか。",
    "code": "class Calc {\n    void print(int x) {\n        System.out.print(\"int \");\n    }\n    void print(double x) {\n        System.out.print(\"double \");\n    }\n}\n// 実行部\nCalc c = new Calc();\nc.print(10.0);\nc.print(10);",
    "options": [
      "int int ",
      "double double ",
      "double int ",
      "int double "
    ],
    "answerIndex": 2,
    "explanation": "メソッド名が同じで引数の型や数が異なるメソッドを複数定義することを「オーバーロード」と呼びます。引数に `10.0`（小数はデフォルトで `double` 型）を渡して呼び出した場合は `print(double)` が、引数に `10`（整数はデフォルトで `int` 型）を渡した場合は `print(int)` がそれぞれ適したメソッドとしてコンパイラにより判定・実行されます。よって、出力は `double int ` となります。"
  },
  {
    "id": 12,
    "category": "継承とポリモーフィズム",
    "question": "次の親クラス `P` を継承した子クラス `C` において、オーバーライドとして正しい（コンパイルエラーにならない）メソッドの定義はどれですか。",
    "code": "class P {\n    protected void show() {\n        System.out.println(\"P\");\n    }\n}",
    "options": [
      "void show() { System.out.println(\"C\"); }",
      "private void show() { System.out.println(\"C\"); }",
      "public void show() { System.out.println(\"C\"); }",
      "protected int show() { return 0; }"
    ],
    "answerIndex": 2,
    "explanation": "メソッドをオーバーライドする際、子クラスでのアクセス修飾子は「親クラスと同じか、それより公開範囲の広い修飾子」にする必要があります。親が `protected` のため、子クラスでは `protected` または `public` にしなければなりません（パッケージプライベート（なし）や `private` は狭くなるためNG）。また、戻り値の型も親クラスと同じ（または共変戻り値）にする必要があります。したがって、戻り値が `void` かつ修飾子が `public` である `public void show()` の定義が正しいです。"
  },
  {
    "id": 13,
    "category": "Javaの基本",
    "question": "Javaのソースコードファイルとクラス定義に関するルールとして、正しい記述はどれですか。",
    "code": "",
    "options": [
      "1つのファイルには、複数の `public` クラスを定義できる。",
      "ソースコードファイル内に `public` クラスを定義する場合、ファイル名は必ずそのクラス名と一致していなければならない。",
      "ソースコードファイル内に `public` クラスがない場合でも、ファイル名は必ずいずれかのクラス名と完全に一致させる必要がある。",
      "クラスのアクセス修飾子は、ファイル名と無関係に自由に設定できる。"
    ],
    "answerIndex": 1,
    "explanation": "Javaのルールでは、1つのソースコードファイル（`.java`）に定義できる `public` クラスは「最大で1つ」までです。さらに、`public` クラスを定義する場合、ファイル名は必ずその `public` クラス名と完全に一致していなければなりません（大文字・小文字も区別）。`public` クラスがないファイルの場合は、任意のファイル名をつけることができます。"
  },
  {
    "id": 14,
    "category": "制御構造",
    "question": "次のコードを実行したときの出力結果として、正しいものはどれですか。",
    "code": "int count = 0;\nfor (int i = 0; i < 5; i++) {\n    if (i % 2 == 0) {\n        continue;\n    }\n    count++;\n}\nSystem.out.println(count);",
    "options": [
      "2",
      "3",
      "5",
      "0"
    ],
    "answerIndex": 0,
    "explanation": "`for` ループは `i = 0` から `i = 4` まで 5 回繰り返されます。ループ内では、`i % 2 == 0`（`i` が偶数のとき）に `continue` が実行されるため、以降の `count++` の処理をスキップして次の繰り返しに進みます。したがって、`count` がインクリメントされるのは `i` が奇数のとき（`i = 1` と `i = 3` の2回のみ）です。結果として出力は `2` となります。"
  },
  {
    "id": 15,
    "category": "継承とポリモーフィズム",
    "question": "次のコードを実行したときの出力結果として、正しいものはどれですか。",
    "code": "class Base {\n    Base() {\n        System.out.print(\"Base \");\n    }\n}\nclass Sub extends Base {\n    Sub() {\n        super();\n        System.out.print(\"Sub \");\n    }\n}\n// 実行部\nSub s = new Sub();",
    "options": [
      "Sub ",
      "Base ",
      "Base Sub ",
      "Sub Base "
    ],
    "answerIndex": 2,
    "explanation": "子クラス（派生クラス）のコンストラクタの先頭行には、明示的に `super()` を記述して親クラスのコンストラクタを呼び出すことができます（記述しなくてもコンパイラが自動で `super();` を挿入します）。これにより、子クラスのインスタンス化時にはまず親クラスのコンストラクタが実行され、その後に子クラスのコンストラクタ内の処理が実行されます。結果として `Base Sub ` と出力されます。"
  },
  {
    "id": 16,
    "category": "データの操作",
    "question": "次のコードを実行したときの出力結果として、正しいものはどれですか。",
    "code": "String s1 = \"Java\";\nString s2 = \"Java\";\nString s3 = new String(\"Java\");\nSystem.out.print((s1 == s2) + \" \" + (s1 == s3));",
    "options": [
      "true true",
      "true false",
      "false true",
      "false false"
    ],
    "answerIndex": 1,
    "explanation": "ダブルクォーテーションで作成した文字列リテラルは、メモリ上の「文字列リテラルプール（定数プール）」にキャッシュされ、再利用されます。そのため、`s1` と `s2` は同一のインスタンスを指し、`s1 == s2` は `true` になります。一方、`new String(\"Java\")` と作成した場合はプールを利用せず、新しくヒープ領域にオブジェクトが生成されるため、`s1` と `s3` は異なる参照先となり、`s1 == s3` は `false` と判定されます（値の比較には `equals()` メソッドを使う必要があります）。"
  }
]
